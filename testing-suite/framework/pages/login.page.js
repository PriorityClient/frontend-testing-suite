export default const LoginPage = {
  Goto,
  CreateNewUser,
  Login,
  Logout,
  ForgotPassword
}

function Goto(){
  // if user is logged in, call Logout();

}

function CreateNewUser(){
}

function Login(){
  // if user is already logged in, call Logout();
}

function Logout(){
  // if user is not logged in, warn
}

function ForgotPassword(){
}
