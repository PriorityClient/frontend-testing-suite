const prefix = ".app-container__modal.modal"
export const modal = by.css(prefix);
export const logoutButton = by.css(`${prefix} .button--action`);
export const cancelButton = by.css(`${prefix} .button--clear`);
